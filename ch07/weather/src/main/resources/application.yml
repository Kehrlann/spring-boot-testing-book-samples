spring:
  application:
    name: "Weather App"
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  docker:
    compose:
      enabled: false
preferences:
  defaults:
    units: metric